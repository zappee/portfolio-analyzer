package com.remal.portfolio.parser;

import com.remal.portfolio.model.ProductPrice;
import org.apache.poi.ss.formula.eval.NotImplementedException;

import java.util.List;

/**
 * Parse files keep product price history.
 * <p>
 * Copyright (c) 2020-2021 Remal Software and Arnold Somogyi All rights reserved
 * BSD (2-clause) licensed
 * </p>
 * @author arnold.somogyi@gmail.comm
 */
public class ProductPriceParser extends Parser<ProductPrice> {

    /**
     * Process a CSV file.
     *
     * @param file path to the data file
     * @return the list of the parsed items
     */
    @Override
    protected List<ProductPrice> parseCsvFile(String file) {
        throw new NotImplementedException(null);
    }

    /**
     * Process an Excel file.
     *
     * @param file path to the data file
     * @return the list of the parsed items
     */
    @Override
    protected List<ProductPrice> parseExcelFile(String file) {
        throw new NotImplementedException(null);
    }

    /**
     * Process a Text/Markdown file.
     *
     * @param file path to the data file
     * @return the list of the parsed items
     */
    @Override
    protected List<ProductPrice> parseMarkdownFile(String file) {
        throw new NotImplementedException(null);
    }
}
